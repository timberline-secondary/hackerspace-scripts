#!/bin/bash
echo "Attempting to install `dialog` package"
sudo apt install dialog beep smbclient
#comment number one
#beep is literally just a package that adds the command "beep" to play a beep sound when executed (for my microwave :D)
